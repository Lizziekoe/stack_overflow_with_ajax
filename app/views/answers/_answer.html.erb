<% if @question.best_answer_id != answer.id %>
  <div class="well answer">
    <div class="col-xs-3 col-md-3">
      <div class="row">
        <%= link_to "Up", vote_path(answer), data: { method: "patch" } %>
        <!-- <span class="glyphicon glyphicon-triangle-top" aria-hidden="true"></span> -->
      </div>
      <div class="row">
        <p><%= answer.votes_for.size %></p>
      </div>
      <div class="row">
        <span class="glyphicon glyphicon-triangle-bottom" aria-hidden="true"></span>
      </div>
        <p><%= answer.content %></p>
    </div>
    <% if current_enjoyer == @question.enjoyer %>
      <p><%= link_to "Select as Best", question_path(@question, question: {best_answer_id: answer.id}), data: { method: "patch" }%></p>
    <% end %>
  </div>
<% end %>
